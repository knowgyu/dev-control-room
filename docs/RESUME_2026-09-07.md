# Quality-first pilot resume — 2026-09-07

The pilot paused on 2026-09-01. Repository status at the 2026-09-07
resume/verification base: local `main` was 10 commits ahead of `origin/main`,
`HEAD` was `2a954048`, and remote `main` was
`b777d97cea5b7216a46f6d9cf1b022b0001f116b`. Version remains `0.15.2`.
Remote verification confirmed v0.15.2 was published at
`2026-08-31T12:31:59Z`; v0.16.0 is planned, not released. No
`dev-control-room` process was running. Re-resolve HEAD and ahead/behind status
after the reviewed local commit.

The worktree was dirty during verification and contained the reviewed
Go/PowerShell changes. The current regression slice is locally verified with
those changes atop `2a95404`, not as a clean tree. The exact native Full record
is [`verification-summary.json`](../artifacts/verification-resume-20260907/verification-summary.json):
status `PASS`, completed `2026-09-07T22:20:41.7406263+09:00`, all 10 steps
passed, and source commit
`2a954048019ff0048c71034281772c4951d322f0`. The native environment was
PowerShell `7.6.5`, Go `1.26.7 windows/amd64`, Node `v24.15.0`, GCC `16.2.0`,
and Git `2.53.0.windows.1`. Go 1.23 native execution and the Go 1.23
build-tag test were not run.

Script worker evidence also passed on 2026-09-07: Pester 6/6 in 8.56s,
PowerShell parse, and diff check for `scripts/measure-dogfood.ps1` and
`scripts/measurement-contract.tests.ps1`. Launch failure records
`unavailable` with zero samples and a null exit code; canonical temporary
validation precedes replacement, and rejection preserves the existing valid
file.

The completed local history is `297021e` persistence, `f4cf236` UI line
endings, `715b973` UI focus/responsiveness, `03b715e` pilot evidence,
`62812f7` atomic writes, `f5454b8` recovery boundaries, `bf595e6` retry and
cleanup, `19dd875` finalization and cleanup, `cac2376` uncertain-finalization
artifact preservation, and `2a95404` measurement boundaries.

Evidence boundary:

- Historical clean v0.15.2 evidence at `3dec04e`: required `pass`, 58.4%
  statements.
- `artifacts/dogfood-p2-final-v2` at `cac2376`: required `pass`, 58.6%,
  deliberately mixed fixture probes `unknown`, but `dirty` and not current
  `HEAD`. It is not a clean baseline; no +0.2 improvement claim is valid.
- Same-commit measurement comparison is repeatability evidence, not
  before/after code improvement. Coverage is not quality. Future pilot
  evidence must separate defect -> fix -> regression from architecture review.

The 2026-09-07 pilot checkpoint uses a concrete example: restart evidence loss
and an invalid launch-failure manifest, each recorded as defect -> linked
regression test -> fix -> regression result. Raw coverage and same-commit
latency deltas are not improvement evidence. The current regression slice is
complete and locally verified; clean pilot baseline, browser UI acceptance,
and the v0.16.0 release remain pending.

Mutation campaign: **not run**. Causal improvement: **not run/unproven**.
Latest UI acceptance: **not run**.
